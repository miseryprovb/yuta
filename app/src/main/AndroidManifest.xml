<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.pku.canoe.yuta">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true">
        <activity android:name="edu.pku.canoe.yuta.FunctionActivity" />
        <activity android:name="edu.pku.canoe.yuta.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="edu.pku.canoe.yuta.CameraActivity" />
        <activity android:name="edu.pku.canoe.yuta.LoginActivity" />
        <activity android:name="edu.pku.canoe.yuta.RegisterActivity" />
        <activity android:name="edu.pku.canoe.yuta.TestActivity" />
        <activity android:name="edu.pku.canoe.yuta.ShowActivity" />
    </application>

</manifest>